import { defineStore } from 'pinia';

export const useNavigationStore = defineStore('navigation', () => {
  const loadModules = () => {};

  const nextModule = () => {};

  return { loadModules, nextModule };
});
